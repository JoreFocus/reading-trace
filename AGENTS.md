# 阅迹项目协作说明

这个仓库是一套可复用的阅读界面，不是一种固定的伴读理论。Agent 的任务是理解材料的独特张力，再让界面服务于这种张力。

## 默认工作路径

1. 先阅读用户提供的原始材料、伴读记录或复盘标注。
2. 阅读 `docs/build-your-reading-panel.md`，理解阅迹的设计原则。
3. 阅读 `docs/creating-with-agents.md`，确定这份材料适合怎样组织。
4. 只修改 `data/reading.json` 即可生成新的阅迹；字段契约见 `data/reading.schema.json`。
5. 运行 `npm run validate:content` 和 `npm test`。
6. 只有当内容确实要求新的交互关系时，才修改 `templates/reading-trace.html`。`app/page.tsx` 只是默认入口，不承载阅迹设计。

## 三层信息不得混淆

- **原文**：作者或讲者确实表达了什么。放入 `quote`，必要时保留来源。
- **系统判断**：这一段为何重要、在结构中承担什么作用。放入 `context`。
- **读者判断**：读者需要亲自形成什么立场。通过 `prompt` 和页面标记发生。

不要把 Agent 的概括写成原文，不要替读者预先回答问题，也不要为了填满版面制造虚假的结构。

## 创作自由

- 不要求固定章节数、每组条目数或叙事顺序。
- 可以按矛盾、转折、时间、问题升级或认知变化组织内容。
- 可以重命名 `core / support / turn` 的显示文字，但不要随意改变底层值。
- 视觉可以演化，但每个新元素都应推动一种真实的阅读行为。
- 优先保留材料的声音，不要把不同作者处理成同一种“AI 文风”。

## 权利与隐私

公开发布前确认材料允许传播。对受版权保护或私密材料，减少原文复现，只保留必要短引、原创分析、来源与授权说明。无法确认时先保持本地或私有预览。

## 项目边界

本项目受到李继刚先生 `ljg-read` 的阅读陪伴理念启发，但不复刻或重新分发其 Skill。这里沉淀的是阅迹自己的界面方法、内容契约与构建流程。
